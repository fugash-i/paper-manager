name: paper-manager
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytest
  - pip:
    - requests
    - pymupdf
    - notion-client
    - google-genai
    - python-dotenv
    - crossrefapi
    - arxiv
    - streamlit
    - watchdog
